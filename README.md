# University Major Recommendation API

API Node.js متكاملة لنظام توصية التخصصات الجامعية

## المميزات

- ✅ المصادقة والتوثيق (JWT)
- ✅ إدارة الطلاب والمعلمين والجامعات
- ✅ إدارة التخصصات والطلبات
- ✅ الاستبيانات والتوصيات
- ✅ لوحة التحكم والإحصائيات
- ✅ التقارير والنسخ الاحتياطي
- ✅ المحتوى والإشعارات
- ✅ التقويم والمقابلات
- ✅ الرسائل والمحادثات

## التثبيت

```bash
# تثبيت المتطلبات
npm install

# إنشاء ملف .env من .env.example
cp .env.example .env

# تشغيل السيرفر
npm start

# للتطوير (مع nodemon)
npm run dev
```

## إعداد قاعدة البيانات

تأكد من تحديث إعدادات قاعدة البيانات في ملف `.env`:

```env
DB_HOST=mysql5047.site4now.net
DB_NAME=db_abf0c2_umj
DB_USER=abf0c2_umj
DB_PASSWORD=admin123
DB_PORT=3306
```

## بنية API

### Endpoints الأساسية

- `/api/v1/auth` - المصادقة (تسجيل الدخول، التسجيل، معلومات المستخدم)
- `/api/v1/students` - الطلاب
- `/api/v1/teachers` - المعلمون
- `/api/v1/universities` - الجامعات
- `/api/v1/majors` - التخصصات
- `/api/v1/applications` - الطلبات
- `/api/v1/recommendations` - التوصيات (جديد)
- `/api/v1/survey` - الاستبيانات
- `/api/v1/dashboard` - لوحة التحكم
- `/api/v1/admin` - الإدارة
- `/api/v1/reports` - التقارير
- `/api/v1/backup` - النسخ الاحتياطي
- `/api/v1/content` - المحتوى
- `/api/v1/calendar` - التقويم
- `/api/v1/notifications` - الإشعارات
- `/api/v1/messages` - الرسائل
- `/api/v1/interviews` - المقابلات
- `/api/v1/semesters` - الفصول الدراسية
- `/api/v1/university-users` - مستخدمو الجامعات

## المصادقة

يتم استخدام JWT tokens للمصادقة. أرسل token في header:

```
Authorization: Bearer <token>
```

## أمثلة الاستخدام

### تسجيل الدخول

```bash
POST /api/v1/auth/login
{
  "email": "user@example.com",
  "password": "password"
}
```

**ملاحظة:** الـ API يكتشف الدور تلقائياً من البريد الإلكتروني. لا حاجة لإرسال `role` في الطلب.

### الحصول على التوصيات

```bash
GET /api/v1/recommendations
Authorization: Bearer <token>
```

### الحصول على الإحصائيات

```bash
GET /api/v1/dashboard/stats
Authorization: Bearer <token>
```

## Swagger Documentation

تم إضافة واجهة Swagger لتوثيق وتجربة الـ API:

### الوصول إلى Swagger UI

بعد تشغيل السيرفر، افتح المتصفح على:
```
http://localhost:8000/api-docs
```

### المميزات

- 📖 توثيق تفاعلي كامل لجميع الـ endpoints
- 🔐 دعم المصادقة (JWT Authentication)
- 🧪 تجربة الـ API مباشرة من المتصفح
- 📝 أمثلة لجميع الطلبات والاستجابات

للمزيد من المعلومات، راجع: [SWAGGER_GUIDE.md](./SWAGGER_GUIDE.md)

## التطوير

- Node.js 18+
- Express.js
- MySQL2
- JWT
- bcryptjs
- Swagger UI Express
- Swagger JSDoc

## الرخصة

ISC

